#include <stdio.h>
#include <getopt.h>
#include <unistd.h>


int main(int argc, char *argv[]) {
    
    int options = 0;
    char *nthreads = "1";
    char *consvoy = "v";      */Ne compile pas quand je transforme le v en True*/
    char *fichierout = NULL;
    char *fichier1;
    
    
    
    while((options=getopt (argc, argv, "t:co:f"))!=-1){
        switch (options) {
            case 't' :
                nthreads=optarg;
                break;
                
            case 'c':
                consvoy = "c";
                break;
                
            case 'o' :
                fichierout = optarg;
                break;
                
            
            */Trouver comment faire pour les fichiers*/
            
            case 'f' :
                fichier1 = 'f';
                printf(fichier1);
                break;
                
                for(int i=0;i<argc;i++){
                    if argv[i]=="."{
                        int len = i+3;
                        int j = i;
                        while argv[j]!=" "{
                            j-=1;}
                        if argv[j-2]!="-"{
                            char *string;
                            while (j<len){
                                
